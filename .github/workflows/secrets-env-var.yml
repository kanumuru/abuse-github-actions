name: Basic Build Action
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - run: |
          echo "${{ secrets.TEST_SECRET }}"
          
     
      - run: 'echo ${{ secrets.TEST_SECRET }} | base64 -d > .env.production && tail .env.production'

